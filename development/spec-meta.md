# 规范简介
 - 轻量SDD，以文档为核心资产，代码视为LLM对文档的“编译产物”

# 关键字
```
# 规范
# 文档维护
# 说明
```

# 规范
 - 简洁
 - 所有`# 规范`应为确定性指令
 - 所有`# 文档维护`作用域为其所在文档
 - 当规范与需求不符时，修改规范

# 文档维护
  - 简洁
  - 只写当前方案
  - 不写未定事项
  - 不同义反复
  - 正文（`# 说明`以外的部分）不写原因

# 说明
 - 建议在每个文档中追加`# 文档维护`（复制+按需修改）
 - `# 规范`主要在本文档形成自约束
